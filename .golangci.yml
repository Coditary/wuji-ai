run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
  disable:
    - unused

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
